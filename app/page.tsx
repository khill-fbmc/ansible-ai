import Home from "@/components/Home";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ansible AI Playbooks",
};

export default function page() {
  return <Home />;
}
